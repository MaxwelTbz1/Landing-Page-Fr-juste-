// Dans votre state, stockez l'objet vidéo complet au lieu de juste l'ID
const [selectedVideo, setSelectedVideo] = useState<any | null>(null);

// Dans le rendu de l'iframe :
<iframe
  src={
    selectedVideo.type === "instagram"
      ? `https://www.instagram.com{selectedVideo.videoId}/embed`
      : `https://www.youtube.com/embed/${selectedVideo.videoId}?autoplay=1`
  }
  allowFullScreen
  className="w-full h-full"
  style={{ border: 'none' }}
/>


